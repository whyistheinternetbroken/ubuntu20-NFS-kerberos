# This is the sample resolv.conf file. Rename to remove sample or create your own, but be sure to use the same name as what's in the dockerfile.
# In most cases, the host resolv.conf file will override this file. Change to use your domain information.

search centos-ldap.local ntap.local
nameserver xxx.xxx.xxx.xxx
